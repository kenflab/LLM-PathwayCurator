[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[project]
name = "llm-pathway-curator"
version = "0.0.0"
description = "Convert enrichment outputs into verifiable, auditable pathway claims with calibrated abstention."
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
authors = [{name = "Ken Furudate"}]

dependencies = [
  "pandas>=2.0",
  "numpy>=1.24",
  "pydantic>=2.0",
  "tabulate>=0.9",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "ruff>=0.4",
]

bench = [
  "matplotlib>=3.8",
  "pyyaml>=6.0",
  "tqdm>=4.66",
  "synapseclient>=2.0",
]

llm = [
  "openai>=1.0",
  "google-genai",
  "google-generativeai",
]

# Convenience meta-extra: everything you need for paper+LLM+dev in one shot
all = [
  "pytest>=8.0",
  "ruff>=0.4",
  "matplotlib>=3.8",
  "pyyaml>=6.0",
  "tqdm>=4.66",
  "synapseclient>=2.0",
  "openai>=1.0",
  "google-genai",
  "google-generativeai",
]

[project.scripts]
llm-pathway-curator = "llm_pathway_curator.cli:main"

[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = [
  "paper/notebooks/*.ipynb",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
markers = [
  "smoke: developer-facing adapter smoke tests (contract checks)",
]
